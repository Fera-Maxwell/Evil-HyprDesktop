# input.conf — key modifiers, basic mouse/keyboard setup

# BSPWM itself doesn’t configure keyboard layouts
# Use setxkbmap or local X settings
setxkbmap -layout us,tr -option grp:alt_space_toggle,ctrl:nocaps
xset r rate 500 50
